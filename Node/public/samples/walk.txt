hex.Base.upsideDown(false);
hex.Motion.init();

//tripodPlaneWalk: function(step_size, n_steps, direction, stepTime, startingTime, changeOrientation)

hex.Motion.tripodPlaneWalk(120, 4, 0, 1500, 100);
